const Discord = require('discord.js')

module.exports = {
    name: 'metar sää',
    description: 'Metar weather',
    detailedDescription: 'Metar weather API',
    execute(message = Discord.Message.prototype, args = []) {

    },
}
